spring:
  application:
    name: firstSpringBoot
  datasource:
      url: jdbc:mysql://localhost:3306/mystore?createDatabaseIfNotExist=true
      username: root
      password: P@ssword
  jpa:
    show-sql: true
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

